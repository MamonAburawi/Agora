<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_nav_graph"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="com.example.agora.screens.home.Home"
        android:label="Home"
        tools:layout="@layout/home">
        <action
            android:id="@+id/action_home_to_encounter"
            app:destination="@id/encounter"
            app:popUpToInclusive="false" />
    </fragment>
    <fragment
        android:id="@+id/encounter"
        android:name="com.example.agora.screens.encounter.Encounter"
        android:label="Encounter"
        tools:layout="@layout/encounter"/>
</navigation>